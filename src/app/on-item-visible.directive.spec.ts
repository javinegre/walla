import { OnItemVisibleDirective } from './on-item-visible.directive';

describe('OnItemVisibleDirective', () => {
  it('should create an instance', () => {
    const directive = new OnItemVisibleDirective();
    expect(directive).toBeTruthy();
  });
});
